import {Volume2} from 'lucide-react';
export default function AudioButton({text,rate=0.9,label}:{text:string;rate?:number;label?:string}){const speak=()=>{if(!('speechSynthesis'in window))return;window.speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(text);u.lang='fr-FR';u.rate=rate;const voices=window.speechSynthesis.getVoices();const fr=voices.find(v=>v.lang.toLowerCase().startsWith('fr'));if(fr)u.voice=fr;window.speechSynthesis.speak(u)};return <button onClick={speak} className="inline-flex items-center gap-2 rounded-xl bg-slate-900 px-3 py-2 text-sm font-semibold text-white hover:bg-slate-700 dark:bg-white dark:text-slate-900" title="Nghe phát âm"><Volume2 size={16}/>{label}</button>}
